# K-Nearest Neighbors (KNN) Classification using Scikit-Learn

This project demonstrates the implementation of the **K-Nearest Neighbors (KNN)** algorithm using Python’s **Scikit-Learn** library.  
We use the **Iris dataset**, visualize the data, train a KNN classifier, plot decision boundaries, and evaluate accuracy for different values of **K**.

---

## 📌 Objectives
- Load and explore the Iris dataset  
- Visualize feature distributions  
- Train a KNN classifier  
- Plot decision boundaries using `mlxtend`  
- Evaluate accuracy for varying K values  
- Understand distance metrics and feature scaling  

---

## 📂 Dataset: Iris Flower Dataset

Loaded from:

```python
from sklearn.datasets import load_iris
